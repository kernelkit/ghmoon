# A small example config showing how to add secrets such as access tokens.
# This config contains secret additions to the base config should not be
# version-controlled in the wild. Merge it with an additional -f FILE.
repos:
  kernelkit/infix:
    token: github_pat_XXXXX
